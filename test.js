let scale = 1
let defaultAmount = 70
let amountOfDucks = 71
let col = 6
let row = 10


    scale = (defaultAmount / amountOfDucks)
    if (Number.isInteger(col / scale)) {
        if (Number.isInteger(row / scale)) {
            col = col / scale
            row = row / scale
        } else {
            
        }


    } else {
        
    }

